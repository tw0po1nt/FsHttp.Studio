// Fixture for the document-aware status bar: an `.fs` module file. The status bar must read
// `not an .fsx script` while this tab is active (docs/spec/0014-explain-missing-lenses.md,
// Decision 5).

module StatusBarModule

let x = 1
