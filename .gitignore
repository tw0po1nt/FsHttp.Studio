# Editors
.DS_Store
.idea/
*.iml
.vscode/*
!.vscode/launch.json
!.vscode/tasks.json
*.swp
*~

# .NET build output
bin/
obj/

# Node / build output
node_modules/
dist/
out/

# UI test harness — generated when the test HTTP server starts
tests/ui.Tests/fixtures/sidecar.json
*.vsix

# Env
.env
.env.*
!.env.example

# Local CLI state (e.g. gh device-id written when resolving GH_TOKEN on the host)
.local/

# Remote Control / worktree-isolated sessions
.claude/worktrees/

# Claude Code skill symlinks — generated locally by scripts/symlink-skills.sh from the
# canonical .agents/skills/ tree. Not committed (Claude Code doesn't yet read .agents/skills/).
.claude/skills/

# ASD-STE100 source PDF for the simplified-technical-english skill — request your own
# authorized copy per .agents/skills/simplified-technical-english/assets/README.md; ASD's
# terms do not permit redistribution.
.agents/skills/simplified-technical-english/assets/*.pdf

.fake